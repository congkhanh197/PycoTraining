import React from 'react';
import { Text } from 'react-native'

import DiscoveryScreen from "./Discovery.screen"

const DiscoveryContainer = (props) => <DiscoveryScreen {...props}/>;

export default DiscoveryContainer;