import React from 'react';
import { Text } from 'react-native'

import ProfileScreen from "./Profile.screen"

const ProfileContainer = (props) => <ProfileScreen {...props}/>;

export default ProfileContainer;