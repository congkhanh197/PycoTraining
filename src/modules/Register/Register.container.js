import React from 'react';
import { Text } from 'react-native'

import RegisterScreen from "./Register.screen";

const RegisterContainer = (props) => <RegisterScreen {...props}/>;

export default RegisterContainer;